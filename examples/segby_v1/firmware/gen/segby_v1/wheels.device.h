/* GENERATED by BBMcuhub.Gen.WireGen — do not edit. wheels.device.h
   device-hook prototypes — the hand-written contract (§ Firmware hook) */
#ifndef BB_MCUHUB_WHEELS_DEVICE_H
#define BB_MCUHUB_WHEELS_DEVICE_H
#include <stdint.h>
#include <stdbool.h>
#include "wire_contract.h" /* the packed value structs the hooks fill/take */

#ifdef __cplusplus
extern "C" {
#endif

/* Implemented by the hand-authored mcu/wheels.{c,cpp}; the generated glue calls these. */
void wheels_device_setup(void); /* one-time bring-up (pins, peripherals,
                                    FOC/encoder init). May take as long as it
                                    needs: the task watchdog is armed AFTER this
                                    returns, so a slow initFOC/i2c settle is safe. */

void wheels_motor_left_drive(float); /* apply to the plant */
void wheels_motor_right_drive(float); /* apply to the plant */
void wheels_post_control(void); /* OPTIONAL: per-loop telemetry. To provide one, `#define WHEELS_POST_CONTROL_OVERRIDE` before #include'ing wheels.glue.h; else a no-op default is used. */

#ifdef __cplusplus
}
#endif

#endif /* BB_MCUHUB_WHEELS_DEVICE_H */
