/* GENERATED by BBMcuhub.Gen.WireGen — do not edit. blaster.device.h
   device-hook prototypes — the hand-written contract (§ Firmware hook) */
#ifndef BB_MCUHUB_BLASTER_DEVICE_H
#define BB_MCUHUB_BLASTER_DEVICE_H
#include <stdint.h>
#include <stdbool.h>
#include "wire_contract.h" /* the packed value structs the hooks fill/take */

#ifdef __cplusplus
extern "C" {
#endif

/* Implemented by the hand-authored mcu/blaster.{c,cpp}; the generated glue calls these. */
void blaster_device_setup(void); /* one-time bring-up (pins, peripherals,
                                    FOC/encoder init). May take as long as it
                                    needs: the task watchdog is armed AFTER this
                                    returns, so a slow initFOC/i2c settle is safe. */
bool blaster_range_front_read(Range *out); /* bounded read; false on timeout */
bool blaster_pose_read(Imu *out); /* bounded read; false on timeout */
void blaster_status_led_drive(const Led *v); /* apply to the plant */
void blaster_post_control(void); /* OPTIONAL: per-loop telemetry. To provide one, `#define BLASTER_POST_CONTROL_OVERRIDE` before #include'ing blaster.glue.h; else a no-op default is used. */

#ifdef __cplusplus
}
#endif

#endif /* BB_MCUHUB_BLASTER_DEVICE_H */
